// User login
exports.loginUser = async (req, res) => {
  res.status(400).json({ message: 'STILL TODO' });
};

// User logout
exports.logoutUser = async (req, res) => {
  res.status(400).json({ message: 'STILL TODO' });
};
